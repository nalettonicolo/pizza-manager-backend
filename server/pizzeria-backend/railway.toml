# Railway: imposta "Root Directory" = server/pizzeria-backend nel dashboard
[build]
builder = "nixpacks"

[deploy]
startCommand = "node dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
