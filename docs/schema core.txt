-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE core.allergeni (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  nome text NOT NULL,
  icona text NOT NULL,
  ordine integer NOT NULL DEFAULT 0,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT allergeni_pkey PRIMARY KEY (id),
  CONSTRAINT allergeni_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.audit_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  user_id uuid,
  azione text NOT NULL,
  entita text NOT NULL,
  entita_id text,
  meta jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT audit_logs_pkey PRIMARY KEY (id),
  CONSTRAINT audit_logs_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id),
  CONSTRAINT audit_logs_user_id_fkey FOREIGN KEY (user_id) REFERENCES core.users(id)
);
CREATE TABLE core.categorie (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  nome text NOT NULL,
  slug text NOT NULL,
  ordine integer NOT NULL DEFAULT 0,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT categorie_pkey PRIMARY KEY (id),
  CONSTRAINT categorie_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.configurazione_costi (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL UNIQUE,
  costo_impasto numeric NOT NULL,
  costo_energia numeric NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT configurazione_costi_pkey PRIMARY KEY (id),
  CONSTRAINT configurazione_costi_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.cottura (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  nome text NOT NULL,
  ordine integer NOT NULL DEFAULT 0,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT cottura_pkey PRIMARY KEY (id),
  CONSTRAINT cottura_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.formati (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  nome text NOT NULL,
  ordine integer NOT NULL DEFAULT 0,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  prezzo numeric NOT NULL DEFAULT 0,
  CONSTRAINT formati_pkey PRIMARY KEY (id),
  CONSTRAINT formati_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.impasti (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  nome text NOT NULL,
  costo_base numeric NOT NULL DEFAULT 0,
  ordine integer NOT NULL DEFAULT 0,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT impasti_pkey PRIMARY KEY (id),
  CONSTRAINT impasti_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.ingredienti (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL,
  costo_unitario numeric NOT NULL,
  unita_misura text,
  tenant_id uuid NOT NULL,
  attivo boolean DEFAULT true,
  deleted_at timestamp with time zone,
  ordine integer NOT NULL DEFAULT 0,
  va_in_cottura boolean DEFAULT false,
  CONSTRAINT ingredienti_pkey PRIMARY KEY (id),
  CONSTRAINT ingredienti_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.ordini (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  numero integer NOT NULL,
  stato USER-DEFINED DEFAULT 'IN_ATTESA'::core.stato_ordine,
  totale numeric NOT NULL,
  tenant_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT ordini_pkey PRIMARY KEY (id),
  CONSTRAINT ordini_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.prodotti (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL,
  prezzo numeric,
  tenant_id uuid NOT NULL,
  costo_base_produzione numeric NOT NULL DEFAULT 0,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  deleted_at timestamp with time zone,
  categoria_id uuid,
  immagine_url text,
  ordine integer NOT NULL DEFAULT 0,
  descrizione text,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT prodotti_pkey PRIMARY KEY (id),
  CONSTRAINT prodotti_categoria_id_fkey FOREIGN KEY (categoria_id) REFERENCES core.categorie(id),
  CONSTRAINT prodotti_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.prodotto_allergeni (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  prodotto_id uuid NOT NULL,
  allergene_id uuid NOT NULL,
  CONSTRAINT prodotto_allergeni_pkey PRIMARY KEY (id),
  CONSTRAINT prodotto_allergeni_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id),
  CONSTRAINT prodotto_allergeni_prodotto_id_fkey FOREIGN KEY (prodotto_id) REFERENCES core.prodotti(id),
  CONSTRAINT prodotto_allergeni_allergene_id_fkey FOREIGN KEY (allergene_id) REFERENCES core.allergeni(id)
);
CREATE TABLE core.prodotto_ingrediente (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  prodotto_id uuid NOT NULL,
  ingrediente_id uuid NOT NULL,
  quantita numeric NOT NULL,
  ordine integer NOT NULL DEFAULT 0,
  CONSTRAINT prodotto_ingrediente_pkey PRIMARY KEY (id),
  CONSTRAINT prodotto_ingrediente_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id),
  CONSTRAINT prodotto_ingrediente_prodotto_id_fkey FOREIGN KEY (prodotto_id) REFERENCES core.prodotti(id),
  CONSTRAINT prodotto_ingrediente_ingrediente_id_fkey FOREIGN KEY (ingrediente_id) REFERENCES core.ingredienti(id)
);
CREATE TABLE core.punti_vendita (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  nome text NOT NULL,
  slug text,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT punti_vendita_pkey PRIMARY KEY (id),
  CONSTRAINT punti_vendita_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.riga_ordine (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  ordine_id uuid NOT NULL,
  prodotto_id uuid NOT NULL,
  quantita integer NOT NULL,
  prezzo numeric NOT NULL,
  CONSTRAINT riga_ordine_pkey PRIMARY KEY (id),
  CONSTRAINT riga_ordine_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id),
  CONSTRAINT riga_ordine_ordine_id_fkey FOREIGN KEY (ordine_id) REFERENCES core.ordini(id),
  CONSTRAINT riga_ordine_prodotto_id_fkey FOREIGN KEY (prodotto_id) REFERENCES core.prodotti(id)
);
CREATE TABLE core.subscriptions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL UNIQUE,
  stripe_customer_id text,
  stripe_subscription_id text,
  piano USER-DEFINED DEFAULT 'FREE'::core.piano_saas,
  stato USER-DEFINED DEFAULT 'ATTIVA'::core.stato_subscription,
  rinnovo_il timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT subscriptions_pkey PRIMARY KEY (id),
  CONSTRAINT subscriptions_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE core.tenants (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL,
  slug text NOT NULL UNIQUE,
  piano USER-DEFINED DEFAULT 'FREE'::core.piano_saas,
  attivo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  deleted_at timestamp with time zone,
  logo_url text,
  indirizzo text,
  telefono text,
  email text,
  lat numeric,
  lng numeric,
  orari_settimana jsonb DEFAULT '[]'::jsonb,
  parametri_operativi jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT tenants_pkey PRIMARY KEY (id)
);
CREATE TABLE core.users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email text NOT NULL UNIQUE,
  password text DEFAULT ''::text,
  nome text,
  ruolo USER-DEFINED NOT NULL,
  tenant_id uuid NOT NULL,
  attivo boolean DEFAULT true,
  last_login timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  deleted_at timestamp with time zone,
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);