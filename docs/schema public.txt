-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.ConfigurazioneCosti (
  id text NOT NULL,
  tenantId text NOT NULL,
  costoImpasto double precision NOT NULL,
  costoEnergia double precision NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT ConfigurazioneCosti_pkey PRIMARY KEY (id),
  CONSTRAINT ConfigurazioneCosti_tenantId_fkey FOREIGN KEY (tenantId) REFERENCES public.Tenant(id)
);
CREATE TABLE public.Ingrediente (
  id text NOT NULL DEFAULT gen_random_uuid(),
  nome text NOT NULL,
  costo double precision NOT NULL,
  tenantId text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  attivo boolean DEFAULT true,
  costoAbbondante numeric,
  costoSenza numeric,
  costoPoco numeric,
  vaInCottura boolean DEFAULT false,
  CONSTRAINT Ingrediente_pkey PRIMARY KEY (id),
  CONSTRAINT Ingrediente_tenantId_fkey FOREIGN KEY (tenantId) REFERENCES public.Tenant(id)
);
CREATE TABLE public.Ordine (
  id text NOT NULL,
  numero integer NOT NULL,
  stato USER-DEFINED NOT NULL DEFAULT 'IN_ATTESA'::"StatoOrdine",
  totale double precision NOT NULL,
  tenantId text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT Ordine_pkey PRIMARY KEY (id),
  CONSTRAINT Ordine_tenantId_fkey FOREIGN KEY (tenantId) REFERENCES public.Tenant(id)
);
CREATE TABLE public.Prodotto (
  id text NOT NULL,
  nome text NOT NULL,
  prezzo double precision NOT NULL,
  tenantId text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT Prodotto_pkey PRIMARY KEY (id),
  CONSTRAINT Prodotto_tenantId_fkey FOREIGN KEY (tenantId) REFERENCES public.Tenant(id)
);
CREATE TABLE public.ProdottoIngrediente (
  id text NOT NULL,
  prodottoId text NOT NULL,
  ingredienteId text NOT NULL,
  quantita double precision NOT NULL,
  CONSTRAINT ProdottoIngrediente_pkey PRIMARY KEY (id),
  CONSTRAINT ProdottoIngrediente_prodottoId_fkey FOREIGN KEY (prodottoId) REFERENCES public.Prodotto(id),
  CONSTRAINT ProdottoIngrediente_ingredienteId_fkey FOREIGN KEY (ingredienteId) REFERENCES public.Ingrediente(id)
);
CREATE TABLE public.RigaOrdine (
  id text NOT NULL,
  ordineId text NOT NULL,
  prodottoId text NOT NULL,
  quantita integer NOT NULL,
  prezzo double precision NOT NULL,
  CONSTRAINT RigaOrdine_pkey PRIMARY KEY (id),
  CONSTRAINT RigaOrdine_ordineId_fkey FOREIGN KEY (ordineId) REFERENCES public.Ordine(id),
  CONSTRAINT RigaOrdine_prodottoId_fkey FOREIGN KEY (prodottoId) REFERENCES public.Prodotto(id)
);
CREATE TABLE public.Tenant (
  id text NOT NULL,
  nome text NOT NULL,
  attivo boolean NOT NULL DEFAULT true,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT Tenant_pkey PRIMARY KEY (id)
);
CREATE TABLE public.User (
  id text NOT NULL,
  nome text NOT NULL,
  email text NOT NULL,
  password text NOT NULL,
  ruolo USER-DEFINED NOT NULL,
  tenantId text NOT NULL,
  createdAt timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT User_pkey PRIMARY KEY (id),
  CONSTRAINT User_tenantId_fkey FOREIGN KEY (tenantId) REFERENCES public.Tenant(id)
);
CREATE TABLE public.clienti (
  id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT clienti_pkey PRIMARY KEY (id),
  CONSTRAINT clienti_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id),
  CONSTRAINT clienti_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);
CREATE TABLE public.ingrediente_allergeni (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  tenant_id uuid NOT NULL,
  ingrediente_id uuid NOT NULL,
  allergene_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT ingrediente_allergeni_pkey PRIMARY KEY (id),
  CONSTRAINT fk_ingrediente_allergeni_allergene FOREIGN KEY (allergene_id) REFERENCES core.allergeni(id),
  CONSTRAINT fk_ingrediente_allergeni_ingrediente FOREIGN KEY (ingrediente_id) REFERENCES core.ingredienti(id)
);
CREATE TABLE public.utenti_ruoli (
  user_id uuid NOT NULL,
  ruolo text NOT NULL,
  tenant_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT utenti_ruoli_pkey PRIMARY KEY (user_id),
  CONSTRAINT utenti_ruoli_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT utenti_ruoli_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES core.tenants(id)
);